[
  {
    "type": "function",
    "function": {
      "name": "get_datetime",
      "description": "Current date and time in ISO 8601. Returns {\"datetime\": \"2026-08-16T11:44:35+02:00\"}. GET /datetime[/{offset}]",
      "parameters": {
        "type": "object",
        "properties": {
          "offset": {
            "type": "string",
            "description": "Four-digit UTC offset with optional sign, e.g. \"+0200\", \"-0530\" or \"0100\". Hour-only values like \"1\" are NOT valid. Omit for UTC."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_timestamp",
      "description": "Current Unix timestamp in seconds. Returns {\"timestamp\": 1786873261}. GET /timestamp",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_microtimestamp",
      "description": "Unix timestamp with microsecond precision. Returns {\"microtimestamp\": 1786873474.745043}. GET /microtimestamp",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_timezones",
      "description": "All available timezones. Returns {\"timezones\": [{\"timezone\": \"Europe/Oslo\", \"offset\": \"+0200\"}, ...]} - a list of OBJECTS, not strings. GET /timezones[/{offset}]",
      "parameters": {
        "type": "object",
        "properties": {
          "offset": {
            "type": "string",
            "description": "Four-digit UTC offset to filter by, e.g. \"+0200\"."
          }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_swatch_internet_time",
      "description": "Swatch Internet Time. Returns {\"beat\": \"@444\", \"date\": \"2026-08-16\"}. Note beat is a string with a leading @, not a number. GET /swatchinternettime",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_random_number",
      "description": "Random integer, inclusive. Returns {\"random_number\": 73, \"range\": {\"from\": 1, \"to\": 100}}. No arguments gives 1-6. Passing only \"to\" fixes the lower bound at 1. GET /random_number[/{from}[/{to}]]",
      "parameters": {
        "type": "object",
        "properties": {
          "from": { "type": "integer", "description": "Lower bound, inclusive." },
          "to": { "type": "integer", "description": "Upper bound, inclusive." }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_random_color",
      "description": "Random hex colour. Returns {\"random_color\": \"#9b6bbf\"}. GET /random_color",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_uuid",
      "description": "Generate a UUID v4. Returns {\"uuid\": \"429151ee-82a1-4438-b2f1-b6b9c9e4a41f\"}. GET /uuid",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_guid",
      "description": "Generate a GUID. Returns {\"guid\": \"...\"}. GET /guid",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_password",
      "description": "Random password including punctuation, 12 characters by default. Returns {\"password\": \"jFehS]AKGx9wl[jp\", \"password_length\": 16}. GET /password[/{length}]",
      "parameters": {
        "type": "object",
        "properties": {
          "length": { "type": "integer", "description": "Password length. Defaults to 12." }
        },
        "required": []
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base64_encode",
      "description": "Encode text to Base64. Returns {\"base64_encoded_data\": \"SGVsbG8gd29ybGQ=\"}. POST /base64_encode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Text to encode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base64_decode",
      "description": "Decode Base64. Returns the raw decoded bytes as application/octet-stream, NOT JSON - unless the request sends Accept: application/json, in which case it returns {\"type\": \"json\"|\"binary\", \"decoded_data\": ...}. POST /base64_decode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Base64 string to decode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base58_encode",
      "description": "Encode text to Base58. Returns {\"base58_encoded_data\": \"9Ajdvzr\"}. POST /base58_encode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Text to encode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base58_decode",
      "description": "Decode Base58. Returns raw bytes, or a typed envelope with Accept: application/json. An invalid character returns HTTP 400 with {\"error\": \"Invalid Base58 input.\"}. POST /base58_decode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Base58 string to decode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base32_encode",
      "description": "Encode text to Base32. Returns {\"base32_encoded_data\": \"JBSWY3DP\"}. POST /base32_encode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Text to encode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "base32_decode",
      "description": "Decode Base32. Returns raw bytes, or a typed envelope with Accept: application/json. POST /base32_decode",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "Base32 string to decode." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "jwt_encode",
      "description": "Encode a payload into an HS256 JWT. Returns {\"jwt\": \"eyJ0eXAi...\"}. IMPORTANT: data must be a JSON string, not an object - passing an object returns {\"error\": \"Invalid data provided. Expected a string.\"}. Serialise the payload first. POST /jwt_encode",
      "parameters": {
        "type": "object",
        "properties": {
          "data": {
            "type": "string",
            "description": "The payload as a JSON STRING, e.g. \"{\\\"user\\\":\\\"alice\\\"}\". Not an object."
          },
          "secret": { "type": "string", "description": "Secret key used to sign the token." }
        },
        "required": ["data", "secret"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "jwt_decode",
      "description": "Decode a JWT. Returns {\"decoded_payload\": {...}}. POST /jwt_decode",
      "parameters": {
        "type": "object",
        "properties": {
          "data": { "type": "string", "description": "The JWT token string." },
          "secret": { "type": "string", "description": "Secret key used to sign it." }
        },
        "required": ["data", "secret"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "qrcode_encode",
      "description": "Generate a QR code. Returns {\"qrcode_image\": \"iVBORw0KGgo...\", \"image_type\": \"png\"}. The request field is \"payload\", not \"data\". POST /qrcode_encode",
      "parameters": {
        "type": "object",
        "properties": {
          "payload": { "type": "string", "description": "Text or URL to encode. Field name is payload, not data." }
        },
        "required": ["payload"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "qrcode_decode",
      "description": "Decode a QR code image. Returns {\"qrcode_content\": \"https://example.com\"}. The request field is \"payload\", not \"data\". POST /qrcode_decode",
      "parameters": {
        "type": "object",
        "properties": {
          "payload": { "type": "string", "description": "Base64-encoded QR image. Field name is payload, not data." }
        },
        "required": ["payload"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hash_md5",
      "description": "MD5 hash. Returns {\"md5_hash\": \"8b1a9953c4611296a827abf8c47804d7\"} - the key is md5_hash, not hash. POST /md5_hash",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "String to hash." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hash_sha1",
      "description": "SHA1 hash. Returns {\"sha1_hash\": \"...\"} - the key is sha1_hash, not hash. POST /sha1_hash",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "String to hash." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hash_sha256",
      "description": "SHA256 hash. Returns {\"sha256_hash\": \"...\"} - the key is sha256_hash, not hash. POST /sha256_hash",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "String to hash." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hash_sha512",
      "description": "SHA512 hash. Returns {\"sha512_hash\": \"...\"} - the key is sha512_hash, not hash. POST /sha512_hash",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "String to hash." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "crc32_checksum",
      "description": "CRC32 checksum. Returns {\"crc32_checksum\": 4157704578} - an integer, not a hex string. POST /crc32_checksum",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "String to checksum." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ping",
      "description": "Connectivity check. Returns {\"ping\": \"pong\"} - the key is ping, not response. GET /ping",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "health_check",
      "description": "Health check. Returns {\"status\": \"ok\", \"microtimestamp\": 1786873258.589068} - the second key is microtimestamp, not timestamp. GET /health",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_client_ip",
      "description": "Public IP address of the caller. Returns {\"ip\": \"203.0.113.42\"}. GET /client_ip",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "get_user_agent",
      "description": "The User-Agent string the API saw. Returns {\"user_agent\": \"curl/8.5.0\"}. GET /user_agent",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ip_reverse_lookup",
      "description": "Geolocate an IP. Returns {\"ip\", \"country\", \"city\", \"location\": {\"lat\", \"lng\"}, \"place\", \"timezone\"}. city and place are frequently null, and coordinates fall back to the country centroid. GET /ip_reverse_lookup/{ip}",
      "parameters": {
        "type": "object",
        "properties": { "ip": { "type": "string", "description": "IP address, e.g. \"8.8.8.8\"." } },
        "required": ["ip"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "domain_ip_lookup",
      "description": "Resolve a domain to an IP. Returns {\"domain\": \"example.com\", \"ip\": \"104.20.23.154\"}. GET /domain_ip_lookup/{domain}",
      "parameters": {
        "type": "object",
        "properties": { "domain": { "type": "string", "description": "Domain name, e.g. \"example.com\"." } },
        "required": ["domain"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "storage_set",
      "description": "Store data for 24 hours. Returns {\"storage_id\": \"...\", \"expire_timestamp\": ...} - the key is storage_id, not uuid. The request body is stored verbatim, so whatever is sent is exactly what storage_get returns. POST /storage",
      "parameters": {
        "type": "object",
        "properties": { "data": { "description": "Value to store. Sent as the request body verbatim." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "storage_get",
      "description": "Retrieve stored data, returned verbatim. An unknown or expired id returns {\"error\": \"Storage id unknown\"}. GET /storage/{storage_id}",
      "parameters": {
        "type": "object",
        "properties": { "storage_id": { "type": "string", "description": "The storage_id returned by storage_set." } },
        "required": ["storage_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "shorten_url",
      "description": "Shorten a URL for 24 hours. Returns {\"short_url\": \"https://307.fi/KtNshX2B\", \"expire_timestamp\": ...}. This is a GET with the target URL inline in the path, not a POST. GET /url_shortener/{url}",
      "parameters": {
        "type": "object",
        "properties": { "url": { "type": "string", "description": "Full URL to shorten." } },
        "required": ["url"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_capture_create",
      "description": "Open a webhook capture session. Returns {\"ok\", \"capture_id\", \"update_url\", \"read_url\", \"expire_timestamp\"}. Point any HTTP client at update_url, then read it back with webhook_capture_read. POST /webhook_capture",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_capture_read",
      "description": "Read a captured request. Returns {\"ok\", \"capture_id\", \"captured_at_timestamp\", \"captured_at_datetime\", \"request\": {method, uri, headers, client_ip, body}}. GET /webhook_capture/{capture_id}",
      "parameters": {
        "type": "object",
        "properties": { "capture_id": { "type": "string", "description": "The capture_id from webhook_capture_create." } },
        "required": ["capture_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_action_create",
      "description": "Create a human-in-the-loop form. Returns {\"ok\", \"action_id\", \"form_url\", \"result_url\", \"expire_timestamp\", \"expire_datetime\"}. Send form_url to a human, then poll webhook_action_result. POST /webhook_action",
      "parameters": {
        "type": "object",
        "properties": {
          "title": { "type": "string", "description": "Title shown on the form." },
          "description": { "type": "string", "description": "Optional explanatory text." },
          "fields": {
            "type": "array",
            "description": "Form fields.",
            "items": {
              "type": "object",
              "properties": {
                "type": { "type": "string", "enum": ["radio", "select", "text", "textarea", "checkbox"] },
                "name": { "type": "string" },
                "label": { "type": "string" },
                "required": { "type": "boolean" },
                "placeholder": { "type": "string" },
                "max_length": { "type": "integer" },
                "options": {
                  "type": "array",
                  "description": "For radio and select. Accepts plain strings or {value, label} objects.",
                  "items": {
                    "type": "object",
                    "properties": {
                      "value": { "type": "string" },
                      "label": { "type": "string" }
                    }
                  }
                }
              },
              "required": ["type", "name", "label"]
            }
          }
        },
        "required": ["title", "fields"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_action_result",
      "description": "Poll for the answer to an action. Returns {\"ok\", \"action_id\", \"status\": \"pending\"|\"answered\", \"created_at_timestamp\", \"created_at_datetime\", \"expire_timestamp\", \"expire_datetime\", \"answered_at_timestamp\", \"answered_at_datetime\", \"response\"}. GET /webhook_action/{action_id}",
      "parameters": {
        "type": "object",
        "properties": { "action_id": { "type": "string", "description": "The action_id from webhook_action_create." } },
        "required": ["action_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_solana_wallet",
      "description": "Generate a Solana wallet. Returns {\"private_key\", \"public_address\"}. FOR DEVELOPMENT AND TESTING ONLY - a key produced by a public HTTP endpoint has crossed a network you do not control. Never fund it. GET /solana/generate_new_wallet",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_bitcoin_wallet",
      "description": "Generate a Bitcoin wallet. Returns {\"private_key\", \"private_key_wif\", \"public_address\"}. FOR DEVELOPMENT AND TESTING ONLY. Never fund it. GET /bitcoin/generate_new_wallet",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "generate_ethereum_wallet",
      "description": "Generate an Ethereum wallet. Returns {\"private_key\", \"public_address\"}. FOR DEVELOPMENT AND TESTING ONLY. Never fund it. GET /ethereum/generate_new_wallet",
      "parameters": { "type": "object", "properties": {}, "required": [] }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "solana_balance",
      "description": "Solana balance. Returns {\"wallet\", \"balance_sol\", \"balance_lamports\"}. GET /solana/balance/{address}",
      "parameters": {
        "type": "object",
        "properties": { "address": { "type": "string", "description": "Solana address." } },
        "required": ["address"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "bitcoin_balance",
      "description": "Bitcoin balance. Returns {\"wallet\", \"final_balance_btc\", \"final_balance_sats\"}. GET /bitcoin/balance/{address}",
      "parameters": {
        "type": "object",
        "properties": { "address": { "type": "string", "description": "Bitcoin address." } },
        "required": ["address"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "ethereum_balance",
      "description": "Ethereum balance. Returns {\"wallet\", \"balance_eth\", \"balance_wei\"} where both balances are STRINGS, because Wei routinely exceeds 2^53 and would lose precision as a JSON number. GET /ethereum/balance/{address}",
      "parameters": {
        "type": "object",
        "properties": { "address": { "type": "string", "description": "Ethereum address, 0x-prefixed." } },
        "required": ["address"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "timestamp_convert",
      "description": "Convert one time value into every representation. Accepts unix seconds, unix MILLISECONDS (13+ digits, the Date.now() format - detected automatically), ISO 8601, RFC 2822, or \"now\". Returns {\"input\", \"detected\": \"unix\"|\"unix_ms\"|\"datetime\"|\"now\", \"timestamp\", \"datetime\", \"rfc2822\", \"utc_datetime\"}. Bad input returns HTTP 400. POST /timestamp_convert",
      "parameters": {
        "type": "object",
        "properties": {
          "data": { "type": "string", "description": "The value to convert: unix seconds, unix milliseconds, ISO 8601, RFC 2822, or \"now\"." },
          "offset": { "type": "string", "description": "Optional four-digit UTC offset for the datetime output, e.g. \"+0200\" or \"-0530\". Same form as /datetime." }
        },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "email_validate",
      "description": "Validate an email address: syntax, then DNS. Returns {\"email\", \"valid_syntax\", \"domain\", \"has_mx\", \"mx_hosts\", \"has_address_record\"}. A failing address is a RESULT with valid_syntax false, not an error. has_address_record covers the RFC 5321 A-record fallback, so absent MX alone does not prove the address dead. Does not verify the mailbox exists. POST /email_validate",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "The email address to validate." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "hash_verify",
      "description": "Verify data against a hash. The algorithm is recognized from the hash itself: an integer means crc32 (the form /crc32_checksum returns), hex strings by length (8 crc32, 32 md5, 40 sha1, 64 sha256, 128 sha512). Returns {\"match\", \"algorithm\", \"computed\"} - computed is always included so a mismatch is debuggable. Unrecognized formats return HTTP 400. POST /hash_verify",
      "parameters": {
        "type": "object",
        "properties": {
          "data": { "type": "string", "description": "The data that was hashed." },
          "hash": { "type": "string", "description": "The expected hash: hex string, or integer crc32 as a string." }
        },
        "required": ["data", "hash"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "slugify",
      "description": "Turn text into a URL slug. Scandinavian letters and common Latin diacritics are transliterated by a fixed table (deterministic across machines): \"Blåbærsyltetøy på Ås!\" becomes \"blabaersyltetoy-pa-as\". Returns {\"slug\"}. Input with no sluggable characters returns HTTP 400 rather than an empty slug. POST /slugify",
      "parameters": {
        "type": "object",
        "properties": { "data": { "type": "string", "description": "The text to slugify." } },
        "required": ["data"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_schedule_create",
      "description": "Schedule a webhook: we POST your payload to a URL at a chosen time, with one retry. The timer an agent lacks. Provide delay_seconds OR fire_at (unix timestamp), between 5 seconds and 24 hours from now. Returns {\"schedule_id\", \"status\": \"scheduled\", \"fire_at_timestamp\", \"result_url\"}. The target must be a public http/https URL on port 80/443; private, loopback and reserved addresses are refused, redirects are never followed. Poll result_url to see the outcome. POST /webhook_schedule",
      "parameters": {
        "type": "object",
        "properties": {
          "url": { "type": "string", "description": "Public http/https URL to POST the payload to." },
          "delay_seconds": { "type": "integer", "description": "Fire this many seconds from now (5 to 86400)." },
          "fire_at": { "type": "integer", "description": "Unix timestamp to fire at, as an alternative to delay_seconds." },
          "payload": { "type": "object", "description": "The JSON body to POST. Up to 32 KB." }
        },
        "required": ["url"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "webhook_schedule_result",
      "description": "Check a scheduled webhook. Returns {\"status\": \"scheduled\"|\"fired\"|\"failed\", \"attempts\", \"http_status\", \"response_excerpt\"}. \"fired\" means a delivery was attempted and the target answered with some HTTP status, recorded in http_status - not that it returned 2xx. Readable for 24 hours after the final attempt. GET /webhook_schedule/{schedule_id}",
      "parameters": {
        "type": "object",
        "properties": { "schedule_id": { "type": "string", "description": "The schedule_id from webhook_schedule_create." } },
        "required": ["schedule_id"]
      }
    }
  },
  {
    "type": "function",
    "function": {
      "name": "validate",
      "description": "Validate a business number by arithmetic - nothing is looked up in any register, so valid means well-formed with a correct check digit, not that it exists. Returns {\"type\", \"valid\", ...per-check fields}. An invalid value is a result with valid false, not an error. The card number is never echoed back. POST /validate/{type}",
      "parameters": {
        "type": "object",
        "properties": {
          "type": { "type": "string", "enum": ["iban", "card", "orgnr", "kontonummer", "phone"], "description": "iban (ISO 13616 mod-97), card (Luhn), orgnr (Norwegian org number MOD11), kontonummer (Norwegian bank account MOD11), phone (E.164 shape)." },
          "data": { "type": "string", "description": "The value to validate. Spaces, dots and dashes are ignored." }
        },
        "required": ["type", "data"]
      }
    }
  }
]
